# Optional local configuration template.
# Copy to .env if you add local-only settings.
#
# Private radar data should not be committed. If needed, keep it outside Git
# and point local scripts to it with a machine-specific path.
#
# RADAR_PRIVATE_DATA_DIR=../private_data
